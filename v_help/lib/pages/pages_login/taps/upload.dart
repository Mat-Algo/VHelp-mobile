import 'package:flutter/material.dart';

class UploadTap extends StatelessWidget {
  const UploadTap({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Center(child: Text("Upload"),),));
  }
}